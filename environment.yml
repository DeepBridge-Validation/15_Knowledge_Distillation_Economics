name: econ-distillation
channels:
  - conda-forge
  - defaults
dependencies:
  # Python version
  - python>=3.9,<3.13

  # Core scientific computing
  - numpy>=1.21.0,<2.0.0
  - pandas>=1.3.0,<3.0.0
  - scipy>=1.7.0,<2.0.0

  # Machine Learning
  - scikit-learn>=1.0.0,<2.0.0
  - xgboost>=1.5.0,<3.0.0

  # Statistics and Econometrics
  - statsmodels>=0.13.0,<1.0.0

  # Visualization
  - matplotlib>=3.4.0,<4.0.0
  - seaborn>=0.11.0,<1.0.0

  # Utilities
  - joblib>=1.1.0,<2.0.0
  - tqdm>=4.62.0,<5.0.0
  - pyyaml>=5.4.0,<7.0.0

  # pip dependencies (not in conda or better from pip)
  - pip
  - pip:
    - openml>=0.12.0,<1.0.0

  # Optional: Jupyter for notebooks
  # - jupyter>=1.0.0
  # - ipykernel>=6.0.0

  # Optional: Development tools
  # - black>=22.0.0
  # - pytest>=7.0.0
  # - flake8>=4.0.0
  # - mypy>=0.950
